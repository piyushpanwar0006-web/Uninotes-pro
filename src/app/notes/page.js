import PlaceholderPage from '@/components/PlaceholderPage';

export default function NotesPage() {
  return <PlaceholderPage title="Study Notes" description="Access full search and filtering for all notes and summaries." />;
}
